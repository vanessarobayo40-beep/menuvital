# Credenciales y configuración real (NUNCA subir a GitHub)
includes/config.php

# Bases de datos locales
database/*.db
database/*.db-journal
database/*.db-wal
database/*.db-shm

# Sistema
*.log
.DS_Store
Thumbs.db

# Material fuente de recetas (documentos de trabajo, no van al servidor —
# lo que sí se sube es lo ya procesado en database/recipes_data.php y assets/img/recetas/)
Recetas2026/
*.docx
*.xlsx
cookies_*.txt

# Artefactos regenerables del pipeline de recetas (tools/build_recipes.py y
# afines) — se reconstruyen corriendo los scripts, no hace falta versionarlos.
tools/__pycache__/
tools/recipes_build.json
tools/recipes_colombianas.json
tools/recipes_validated.json
tools/_build_warnings.txt
